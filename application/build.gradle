group 'mlesiewski'
version '1.0-SNAPSHOT'

apply plugin: 'java'

jar {
    manifest {
        attributes(
                "Manifest-Version": "1.0",
                "Main-Class": "mlesiewski.application.Main"
        )
    }
}